# Mutation testing configuration for iterate

[mutate]

# Directories to mutate
directories = [
    "internal/agent",
    "internal/community",
    "internal/evolution",
    "internal/social",
    "internal/commands",
]

# Test commands to run
[[run]]
name = "go test"
command = "go test -v ./..."
timeout = 60
