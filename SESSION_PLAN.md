## Session Plan\n\nSession Title: Fix Broken CI Tests\n\n### Task 1: Fix undefined NewHelp in help_test.go\nFiles: cmd/iterate/commands/help_test.go\nDescription: The test file references `NewHelp(nil)` function which doesn't exist. Since cmd/iterate/commands/ only contains help_test.go with no actual implementation, the tests are testing non-existent code. Remove or stub the NewHelp function calls to fix the build error.\nIssue: None (CI failure)\n\n### Task 2: Fix TestSaveAndLoadPRState failure\nFiles: internal/evolution/engine_extended_test.go\nDescription: The test expects to load PR state with prNumber=42, but the code clears the state when it detects a non-open PR. Need to either mock the PR check or adjust the test to work with the current behavior where pr_state.json is cleared.\nIssue: None (CI failure)\n\n### Task 3: Fix TestSuggester_GetSuggestions_EmptyPrefix\nFiles: internal/suggest/suggest.go\nDescription: The GetSuggestions function returns empty suggestions for empty prefix. The test expects non-nil suggestions. Should return some default suggestions (like common snippets) when prefix is empty.\nIssue: None (CI failure)\n\n### Task 4: Fix TestListGoFiles to exclude test files\nFiles: internal/suggest/suggest.go\nDescription: The listGoFiles function includes test files (files ending with _test.go) but the test expects only non-test Go files. Add a filter to skip files matching \"*_test.go\" pattern.\nIssue: None (CI failure)\n\n### Issue Responses\n- None: implement — These are CI test failures preventing the build from passing\n"}}